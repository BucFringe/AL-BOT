generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model charconfig {
  id      String  @id @default(auto()) @map("_id") @db.ObjectId
  monster String?
  name    String
  task    String
}
